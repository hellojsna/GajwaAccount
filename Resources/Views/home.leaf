<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>#(user.userName)님의 Gajwa Account</title>
    <style>
        @import url("/styles/root.css");
        @import url("/styles/home.css");
    </style>
</head>

<body>
    <div id="homeView">
        <h1 class="heading xlarge">#(user.userName)님, 환영합니다.</h1>
        <h3 class="heading medium">#(user.userLoginID)</h3>
        <hr>
        <h3 class="heading medium">학번 정보</h3>
        #for(userStudentID in userStudentIDMap):
        <p class="body">#(userStudentID.year)학년도 <span class="body bold">#(userStudentID.studentID)</span></p>
        #endfor
        <hr>
        <p>Gajwa Account</p>
    </div>
</body>

</html>